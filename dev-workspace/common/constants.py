from os.path import abspath, dirname, join

CONFIG_DIR = abspath(join(dirname(abspath(__file__)), "../config"))
RESOURCES_DIR = abspath(join(dirname(abspath(__file__)), "../resources"))

RESOURCE_NAME_PREFIX = "DevWorkspace"
